// Modal templates are consumed via `@/entities/modal-template/ui/<flavor>/<modal>`
// namespace imports (e.g. `import * as MessageModal from '@/entities/modal-template/ui/mui/message-modal'`).
// This barrel intentionally re-exports nothing — the slice surface is the directory tree.
export {};
